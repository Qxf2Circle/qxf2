general:
  artifacts:
    - "./test" #Save the screenshots for all the tests
    
## Customize dependencies
dependencies:
  pre:
    - sudo pip install -U selenium
    - sudo pip install python-dotenv	
