general:
  artifacts:
    - "./screenshots" #Save the screenshots for all the tests
    
## Customize dependencies
dependencies:
  pre:
    - sudo pip install -U selenium
    
## Running Tests
test:
   override:
    - python Navigate_Qxf2_Tutorial_Test.py
