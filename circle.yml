## Customize the test machine
machine:

  timezone:
    America/Los_Angeles # Set the timezone


## Customize dependencies
dependencies:
  pre:
    - sudo pip install -U selenium
    - sudo pip install python-dotenv	
test:
  override:
	- chmod +x DropTask/DropTask.sh
	- DropTask/DropTask.sh

